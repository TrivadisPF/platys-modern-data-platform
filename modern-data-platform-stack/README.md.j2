{% set ns = namespace(sep='') -%}
# Platform `{{services['markdown-renderer'].environment.PLATYS_PLATFORM_NAME}}`

This is a docker compose stack generated by [Platys](https://github.com/TrivadisPF/platys-modern-data-platform) with the [ {{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK}} ](https://hub.docker.com/repository/docker/{{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK}}) with the `{{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK_VERSION}}` version.

It contains the following services:

<br>
{%- for service in services.items() | sort() %}
 {%- set ns.sep = " - " -%}
 {%- set ns.space = " " -%}
 {%- if service[1].init is not defined %}
  *{{ns.space}}{%- if service[1].labels and service[1].labels['com.platys.name'] is defined -%} [{{ service[0]}}](https://github.com/TrivadisPF/platys-modern-data-platform/tree/master/documentation/services/{{service[1].labels['com.platys.name'] }}.md) {%- else -%} {{ service[0]}} {%- endif -%}  
    {%- if service[1].labels and service[1].labels['com.platys.description'] is defined -%} {{ns.sep}} {{service[1].labels['com.platys.description'] }} {%- endif -%} 
{%- endif -%} 
{%- endfor -%}
<br>
_**Note**: the init container·("init:·true")·are·not·shown_

**Links:**

 * [docker-compose.yml](./docker-compose.yml)
 * [config.yml](./config.yml)