{% set ns = namespace(sep='') -%}
#· Platform `{{services['markdown-renderer'].environment.PLATYS_PLATFORM_NAME}}`

This is a docker compose stack generated by [Platys](https://github.com/TrivadisPF/platys-modern-data-platform) with the services listed below.

It has been generated using the [ {{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK}} ](https://hub.docker.com/repository/docker/{{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK}}) using version `{{services['markdown-renderer'].environment.PLATYS_PLATFORM_STACK_VERSION}}`.

| Service | External<br>Port | Internal<br>Port | Description
|--------------|------|------|------|------------
{%- for service in services.items() | sort() %}
 {%- set ns.sep = "" -%}
 {%- if service[1].init is not defined %}
| {%- if service[1].labels and service[1].labels['com.platys.name'] is defined -%} [{{ service[0]}}](https://github.com/TrivadisPF/platys-modern-data-platform/tree/master/documentation/services/{{service[1].labels['com.platys.name'] }}.md) {%- else -%} {{ service[0]}} {%- endif -%}  
|{%- for port in service[1].ports -%}
{{port.split(':')[0]}}<br>
  {%- endfor -%}
|{%- for port in service[1].ports -%}
{{port.split(':')[1]}}<br>
  {%- endfor -%}
| {%- if service[1].labels and service[1].labels['com.platys.description'] is defined -%} {{service[1].labels['com.platys.description'] }} {%- endif -%} 
{%- endif -%} 
{%- endfor -%} |

_**Note**: init container·("init:·true")·are·not·shown_

**Links:**

 * [docker-compose.yml](./docker-compose.yml)
 * [config.yml](./config.yml)