connector.name=postgresql
connection-url=jdbc:postgresql://${ENV:HIVE_METASTORE_DB_HOST}:${ENV:HIVE_METASTORE_DB_PORT}/${ENV:HIVE_METASTORE_DB_NAME}
connection-user=${ENV:HIVE_METASTORE_DB_USER}
connection-password=${ENV:HIVE_METASTORE_DB_PASSWORD}

case-insensitive-name-matching=true