[default]
aws_access_key_id = ${AWS_ACCESS_KEY}
aws_secret_access_key = ${AWS_SECRET_ACCESS_KEY}

# necessary if the file is used as a proerties file for AWSCredentials
accessKey = ${AWS_ACCESS_KEY}
secretKey = ${AWS_SECRET_ACCESS_KEY}